# ice-eduinfo-map(인천교육 공개지도) 발전 방향 개선보고서

- 작성일: 2026-07-05
- 점검 범위: `incheon-edu-map/` 전체 (HTML 6종, `js/` 모듈 10종, `data/` 4종, 테스트 6종, 문서 7종)
- 점검 방법: 실제 코드·커밋 이력·기존 분석문서(`fix-recommendations.md`, `ui-ux-ai-slop-audit.md`, `docs/upgrade-handoff.md`) 교차 대조
- 관점 구성: ① 교육청 담당자(행정·운영) ② 시니어 개발자(아키텍처·품질) ③ 시니어 디자이너(UX·브랜드)

---

## 확정 결정사항 (2026-07-05 사용자 결정)

| # | 쟁점 | 결정 | 보고서 반영 |
|---|------|------|------------|
| D1 | 서비스 지위 | **A안 확정 — 개인 업무도구 유지** (기관 공식 도메인 미사용) | §2-2 ② B안 보류 해제 불필요, §4-2 ⑤ NEWSKOOL4D 브랜드 유지 확정 |
| D2 | 아키텍처 | **단일화 확정 — 신형(unified-map) 승격안 채택** | §3-2 ① 로드맵 실행 확정, 동결안 폐기 |
| D3 | 디자인 | **DESIGN.md 이행 격차 해소 진행** | §4-2 ① 단일화 로드맵과 묶어 실행 |
| D4 | 데이터 출처 | **교육청 홈페이지 공식 파일 준용** (2026.4.1. 기준) | §2-2 ① 출처 표기 문구 확정, 갱신은 공식 파일 갱신 시점 추종 |
| D5 | CI 반영 (추가) | **인천교육청 CI 파생 색상 체계 채택** — 색상 차용 O, 심벌·로고 게재 X | §4-2 ⑤ 재결정 완료: DESIGN.md §2 토큰 CI 정렬·§8 CI 사용 규정 신설, 옐로 아이덴티티 폐기 |

- 화면 고지 확정 문구(안): "본 지도는 개인 제작 참고자료로 공식 자료가 아닙니다. 학교 현황은 인천광역시교육청 홈페이지 공개 자료(2026.4.1. 기준)를 준용합니다."

---

## 0. 요약 (결론 먼저)

| 관점 | 현재 평가 | 최우선 과제 1개 |
|------|-----------|----------------|
| 교육청 담당자 | 실무 활용 가치 높음. 공개/비공개 경계 설계 우수. 데이터 거버넌스 부재 | **데이터 기준일·갱신 절차 공식화** (연 2회, 학기 기준) |
| 시니어 개발자 | 솔로 프로젝트 대비 높은 성숙도(모듈화·테스트·QA 픽스처). 아키텍처 이중화가 최대 부채 | **구형 4페이지 vs 신규 모듈러 아키텍처 단일화 결단** |
| 시니어 디자이너 | 디자인 시스템(DESIGN.md) 문서는 완성. 구현 이행률이 페이지별 불균등 | **DESIGN.md 마이그레이션 체크리스트 실행** (unified-map만 준수 중) |

**종합 판정**: 프로젝트는 "기능 추가" 단계가 아니라 **"단일화·이행·공식화" 단계**. 새 기능보다 이미 설계된 것을 완성하는 데 자원을 집중할 시점.

---

## 1. 현황 진단 (실측 기반)

### 1-1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 성격 | GitHub Pages 정적 지도 웹앱 (빌드 단계 없음) |
| 지도 엔진 | Kakao Maps JavaScript SDK (도메인 제한 JS 키, 브라우저 localStorage 저장) |
| 데이터 | 관내 960개교(2026.4.1 기준, 좌표 사전계산), 체험교육기관, 교육지원청 6곳 |
| 공개 페이지 | `index.html`(랜딩) → `schools.html`(학교 디렉토리), `infrastructure.html`(체험기관) |
| 비공개 페이지 | `business.html`, `private-local-map.html` (로컬 전용, 배포 제외) |
| 신규 아키텍처 | `unified-map.html` + `js/` ES 모듈 10종 + 단위테스트 6종 |

### 1-2. 잘 되어 있는 것 (유지·강화 대상)

| 항목 | 근거 |
|------|------|
| 키 보안 설계 | 하드코딩 키 0건. JS 키/REST 키 분리 원칙 및 배포·키 모드 매트릭스가 README에 명문화 |
| 데이터 분리 | `BASE_SCHOOLS` 인라인 하드코딩 → `data/schools.json` 분리 완료. 좌표 사전계산으로 초기 지오코딩 0건 (커밋 `2aa86db`, `75994b0`) |
| 테스트 기반 | `tests/` 단위테스트 6종(`node --test`), 1,000행 합성 QA 픽스처 + 기대값 매니페스트, 구문검사 도구(`tools/check-syntax.mjs`) |
| 기존 권고 반영 | SDK 이중로드 가드(`removeKakaoSdkScripts()`, shared.js:390), resize 핸들러 명명 함수화(shared.js:502-506), 인라인 이벤트 0건·`alert/confirm` 0건 (ui-ux-ai-slop-audit 재측정치 기준) |
| 실패 상태 설계 | 지오코딩 실패 행을 가짜 좌표에 올리지 않고 데이터 상태로 보고. 키 미등록 시에도 나머지 UI 사용 가능 |

### 1-3. 핵심 격차 (본 보고서의 개선 대상)

| # | 격차 | 실측 근거 |
|---|------|-----------|
| G1 | **아키텍처 이중화**: 구형 페이지 4종(각 1,000~1,500줄 인라인 JS, 합계 약 4,900줄)과 신규 모듈러(`unified-map.html` 839줄 + `js/` 모듈)가 병존. GNB는 구형만 링크, `unified-map.html`은 어디서도 진입 불가 | `shared.js:234-241` GNB에 schools/infrastructure만 존재. `grep unified-map` 결과 자기 자신과 index 외 참조 없음 |
| G2 | **디자인 시스템 미이행**: DESIGN.md는 "흰 캔버스·다크/틸 제거"를 규정하나, `index.html`은 다크(`#181716`)+틸/블루 그라디언트, `schools.html`은 다크 slate(`#1e293b`, `#0f172a`) 유지. `unified-map.html`만 준수(`--um-canvas: #ffffff`) | `index.html:16-23`, `schools.html:20-46`, `unified-map.html:11` |
| G3 | **데이터 거버넌스 부재**: "2026.4.1 기준"이 커밋 메시지에만 존재, 화면·데이터 파일에 기준일/출처/갱신주기 미표기 | `data/schools.json` 메타데이터 필드 없음 |
| G4 | **운영 공식화 미비**: `newskool4d.github.io` 개인 계정 배포. 기관 공식 서비스인지 개인 도구인지 지위 불명확 | README 배포 안내 |
| G5 | 잔여 기술부채: `package.json`에 `"type": "module"` 미선언(MODULE_TYPELESS 경고), 루트 중첩 폴더(`ice-eduinfo-map.io-main/ice-eduinfo-map.io-main/`), 루트의 빈 `tests/`·`.agents/` 폴더 | `docs/upgrade-handoff.md` Residual Risk, 폴더 실측 |
| G6 | 실 키 검증 공백: 실제 카카오 JS 키로 지도 타일 렌더링을 검증한 기록 없음 (QA는 fake `window.kakao.maps` 사용) | `docs/upgrade-handoff.md`, `handoff.md` Known Caveats |

---

## 2. 관점 1 — 교육청 담당자 (행정·운영)

### 2-1. 업무 가치 재확인

- 관내학교 디렉토리: 지원청별 필터 + 검색 → 전보·출장·공문 수신처 확인 등 일상 조회 업무 단축
- 체험교육기관 지도: 현장체험학습 계획 수립 시 위치·연락처 일괄 확인 → 학교 지원 문의 대응 근거
- 로컬 비공개 도구: 민감정보(업무용 CSV)를 서버 전송 없이 브라우저 안에서만 처리 — 개인정보 처리 최소화 원칙에 부합하는 구조

### 2-2. 개선 방향

#### ① 데이터 거버넌스 수립 (최우선)

| 항목 | 현재 | 개선안 |
|------|------|--------|
| 기준일 표기 | 커밋 메시지에만 존재 | 화면 하단·데이터 파일에 "학교 현황: 2026.4.1. 기준" 명시 |
| 갱신 주기 | 없음 | 연 2회(3.1./9.1. 학기 개시) 정기 갱신 원칙 수립 — 개교·폐교·이전·교명 변경 반영 |
| 출처 표기 | 없음 | 원천 데이터 출처(학교알리미·교육청 학교 현황 자료 등) 및 좌표 산출 방법 명시 |
| 갱신 절차 | 없음 | "원천 자료 수령 → `build_schools_json.py` 재실행 → 좌표 검수 → 커밋" 절차서 1쪽 작성 |

- 근거: 공공 정보 제공물은 기준 시점이 없으면 민원·오안내 위험. 특히 폐교·통합 미반영 시 이용자 혼선.

#### ② 서비스 지위 명확화

- 현재 개인 GitHub 계정(`newskool4d.github.io`) 배포 → 아래 중 택일 필요:

| 안 | 내용 | 판단 기준 |
|----|------|-----------|
| A. 개인 업무도구 유지 | 현행 유지 + 화면에 "개인 제작 참고자료이며 공식 자료가 아님" 고지 | 기관 승인 절차 없이 즉시 가능. **권장(단기)** |
| B. 기관 공식화 | 부서 검토 → 기관 계정/도메인 이전, 공공 웹 접근성·보안성 검토 절차 | 활용도가 검증된 후 추진. 유권해석·내부 절차 필요 |

- A안 채택 시에도 면책 문구·기준일·출처 3종 세트는 필수.

#### ③ 개인정보·보안 경계 상시화

- 현재 설계(공개/비공개 파일 분리, REST 키 서버 전용 원칙)는 우수 — 문제는 **사람의 실수** 방지 장치 부재.
- 개선안:
  - `.gitignore`에 업무용 CSV 패턴(`*.csv` 중 `data/institutions.sample.csv` 제외) 명시 + 배포 전 체크리스트(`pre-deploy-checklist.md` 4절)에 "민감 파일 스테이징 확인" 항목 추가
  - 공개 배포는 화이트리스트 방식(포함 파일 목록 고정)으로 전환 — 3-2. 개발자 관점 ⑤와 연동

#### ④ 웹 접근성 대비

- 기관 공식화(B안) 추진 시 공공기관 웹사이트는 웹 접근성(KWCAG) 준수 대상 — 지도 기반 서비스는 대체 수단(목록 뷰·표 형태 데이터 열람) 제공이 핵심 쟁점.
- 현재 사이드바 목록이 사실상 대체 수단 역할 → "지도 없이 목록만으로 전체 정보 접근 가능"을 명시적 요건으로 관리하면 대응 용이.

#### ⑤ 활용 확대 (기능 추가가 아닌 안내 강화)

- 신규 전입 교직원 안내자료·업무 인수인계 자료에 지도 URL 포함
- 체험학습 계획 수립 시즌(3~4월, 9~10월) 전 데이터 갱신 일정 연동
- 인쇄용 요약(지원청별 학교 수 표 등)은 별도 개발보다 기존 JSON 내보내기 활용 안내로 충분

### 2-3. 담당자 관점 기대효과

- 데이터 신뢰성 확보 → 오안내 민원 예방
- 담당자 변경 시에도 갱신 절차서 1쪽으로 지속 운영 가능
- 공개 범위 사고(민감 CSV 커밋) 원천 차단

---

## 3. 관점 2 — 시니어 개발자 (아키텍처·품질)

### 3-1. 총평

- 정적 앱 제약(빌드 없음·서버 없음) 아래에서 모듈 분리·테스트·QA 픽스처까지 갖춘 점은 동급 프로젝트 대비 상위 수준.
- 그러나 **한 저장소에 두 세대의 아키텍처가 공존**하는 상태가 모든 후속 작업 비용을 2배로 만들고 있음. 기능보다 이 결단이 먼저.

### 3-2. 개선 방향

#### ① 아키텍처 단일화 (최우선 — 이번 분기 내 결단)

현황:

| 세대 | 구성 | 상태 |
|------|------|------|
| 구형 | `schools.html`(1,101줄)·`infrastructure.html`(1,063줄)·`business.html`(1,261줄)·`private-local-map.html`(1,478줄) — 페이지별 인라인 JS | GNB 연결됨. 실사용 정본 |
| 신형 | `unified-map.html`(839줄) + `js/` ES 모듈 10종(스키마·저장소·임포터·지오코드 큐·레이어) + 테스트 6종 | 진입 경로 없음. 테스트는 신형만 커버 |

권장 로드맵 (신형 승격안):

1. **1단계**: `unified-map.html`을 GNB에 노출(베타 라벨) → 실 키로 스모크 테스트(G6 해소 겸행)
2. **2단계**: `schools.html`·`infrastructure.html`의 기능을 unified-map 필터 프리셋(URL 파라미터 `?view=schools` 등)으로 흡수
3. **3단계**: 구형 공개 페이지 2종 리다이렉트 처리 후 제거. `business.html`·`private-local-map.html`은 로컬 전용이므로 마지막에 이관 또는 동결 선언
4. 각 단계마다 `tests/` 통과 + `pre-deploy-checklist.md` 수행을 게이트로 설정

- 대안(동결안): 신형 개발을 중단하고 구형 4페이지를 정본으로 선언. **비권장** — 테스트 자산·스키마 검증·임포터가 모두 신형에 있어 매몰 비용이 큼.

#### ② 잔여 기술부채 정리 (반나절 분량)

| 항목 | 조치 |
|------|------|
| `package.json` `"type": "module"` 미선언 | 앱 루트에 package.json 추가 선언 → MODULE_TYPELESS 경고 제거 |
| 루트 중첩 폴더 `ice-eduinfo-map.io-main/ice-eduinfo-map.io-main/` | 저장소 구조 평탄화 (git history 유지 위해 `git mv`) |
| 루트의 빈 `tests/`·`.agents/` 폴더 | 용도 확인 후 제거 |
| `fix-recommendations.md` 미결 항목 | 1-2(customSchools 스키마 검증)·2-1(WeakMap)·2-6(innerText) 반영 여부 재감사 — 반영분은 문서에 완료 표기해 문서-코드 괴리 해소 |
| shared.js 이모지 라벨(`❌`, `⏱️`) | DESIGN.md "이모지 라벨 금지" 규정과 충돌 — 텍스트 라벨로 교체 (디자이너 관점 ⑤와 연동) |

#### ③ CI 도입 (이미 가진 것을 자동화)

- 현재 검증 자산이 전부 수동 실행 → GitHub Actions 워크플로 1개로 자동화:

```yaml
# 개념 설계 (push/PR 트리거)
- node tools/check-syntax.mjs
- node --test tests/*.test.mjs
- 민감 파일 패턴 grep 게이트 (CSV·키 문자열 검출 시 실패)
```

- `playwright`가 이미 의존성에 있으므로 fake-kakao 브라우저 QA(`run-integrated-static-qa.mjs`)를 주간 스케줄로 승격 가능.

#### ④ 실 키 스모크 테스트 정례화 (G6)

- fake SDK 기반 QA는 로직 회귀만 보장 — 실제 도메인 등록·타일 렌더링·클러스터러 동작은 미검증 상태.
- 개선안: 배포 후 5분 수동 스모크 절차를 `pre-deploy-checklist.md`에 명문화 (실 키 입력 → 지도 렌더 → 마커 클릭 → 필터 1회 → 키 삭제).

#### ⑤ 배포 파이프라인 안전장치

- 현재 "public-site 폴더 내용만 수동 복사" 방식 → 실수 여지 큼.
- 개선안: 공개 파일 화이트리스트(README 표 그대로)를 스크립트화 → Actions에서 화이트리스트 외 파일이 배포 대상에 있으면 실패. 담당자 관점 ③과 동일 조치.

#### ⑥ Phase 2 지오코딩 프록시 — 착수 기준 제시

- README에 설계 원칙(Vercel 서버 라우트 + REST 키 환경변수)이 이미 명문화되어 있음. **지금 구현하지 않는 것이 맞음.**
- 착수 트리거 정의: "좌표 없는 신규 주소가 1회 갱신에 50건 이상 발생"할 때만 착수. 그 전까지는 좌표 사전계산 방식이 비용·보안 모두 우위.

### 3-3. 개발자 관점 기대효과

- 코드베이스 단일화 → 수정 1회로 전 페이지 반영, 테스트 커버리지가 실서비스와 일치
- CI 게이트 → 민감 파일 유출·구문 오류의 배포 전 차단
- 문서-코드 괴리 해소 → 인수인계 신뢰도 확보

---

## 4. 관점 3 — 시니어 디자이너 (UX·브랜드)

### 4-1. 총평

- `DESIGN.md`는 토큰·컴포넌트·모션·마이그레이션 체크리스트까지 갖춘 완성도 높은 디자인 시스템 문서. **문제는 문서가 아니라 이행률**: 신형 `unified-map.html`만 준수하고, 이용자가 실제 진입하는 `index.html`·`schools.html`은 제거 대상으로 명시된 다크/틸 테마 그대로.
- 결과적으로 이용자는 "다크 랜딩 → 다크 학교지도"를 보고, 흰 캔버스 신형 화면은 볼 수 없는 역설적 상태.

### 4-2. 개선 방향

#### ① DESIGN.md 마이그레이션 실행 (최우선)

- DESIGN.md §7 체크리스트가 이미 실행 목록임 — 신규 설계 불필요, 적용만 필요:

| 체크리스트 항목 | 대상 파일 | 상태 |
|----------------|-----------|------|
| 다크 배경 → 흰 캔버스 | `index.html:16-23`(`#181716`), `schools.html`(`#1e293b`/`#0f172a`) | 미이행 |
| 틸-블루 그라디언트 배지 → 옐로 아이덴티티 + ink 텍스트 | `index.html` brand-mark | 미이행 |
| 블루 채움 CTA → 검정 필 버튼 | 구형 페이지 전반 | 미이행 |
| 이모지 라벨 → 텍스트/칩 | `shared.js`(`❌`, `⏱️` 등) | 미이행 |
| 흰 모달·yellow-soft 키 안내 | `unified-map.html` | **이행 완료 (준거 구현)** |

- 실행 전략: 아키텍처 단일화(개발자 관점 ①)와 **동일 로드맵으로 묶을 것**. 구형 페이지를 다크→흰으로 재도색하는 이중 작업 대신, unified-map 승격 시점에 자연 해소하는 편이 총비용 최소.
- 예외: `index.html`(랜딩)은 승격과 무관하게 남으므로 단독 재도색 필요 — 아래 ②와 함께 처리.

#### ② 정보구조: 랜딩의 역할 축소

- DESIGN.md §1 원칙: "첫 뷰포트에 지도·필터·카운트 노출, 마케팅 랜딩 아님."
- 현재 `index.html`은 카드형 랜딩 → 원칙과 충돌. 개선안 2안:

| 안 | 내용 | 권장 |
|----|------|------|
| A. 얇은 허브 | 랜딩을 2개 지도 진입 카드 + 기준일·출처 고지만 남긴 1스크린 흰 캔버스 페이지로 축소 | **권장** — 담당자 관점 고지 의무와 결합 |
| B. 직행 | index → 통합지도 즉시 리다이렉트, 고지는 지도 헤더로 이동 | 단일화 완료 후 검토 |

#### ③ 접근성 디자인 검증

- 카테고리 파스텔 표면의 명도 대비 실측 필요: `#746019 on #fff4c4`, `#7a3d00 on #ffe6cd` 등은 소형 텍스트(micro 12px) 기준 WCAG AA(4.5:1) 경계선 — 칩 텍스트 크기·굵기와 함께 검증.
- 색상만으로 구분되는 상태(연결선 빨강/파랑, 카테고리 색) → 형태·라벨 이중 부호화 원칙 추가.
- 포커스 링(2px 블루)·ESC 닫기·`aria-live`는 이미 규정·상당 부분 구현 — `pre-deploy-checklist.md` 3절을 릴리스 게이트로 유지.

#### ④ 모바일 UX

- DESIGN.md가 규정한 "사이드바 → 바텀시트 전환"의 실제 구현 품질 점검 필요 (375px 스크린샷은 확보되어 있으나 터치 조작성 검증 기록 없음).
- 점검 항목: 바텀시트 드래그 핸들 크기(44px 터치 타깃), 지도 제스처와 시트 스크롤 충돌, 필터 칩 가로 스크롤.

#### ⑤ 브랜드 정합성

- "NEWSKOOL4D" 개인 브랜드 마크 + 옐로 아이덴티티는 **개인 도구(담당자 관점 A안)에는 적합**.
- 기관 공식화(B안) 추진 시에는 인천교육 브랜드 체계와 정렬 필요 — 그 시점에 옐로 아이덴티티 유지 여부를 재결정. 지금 선제 변경할 필요 없음.
- 카피 톤: "과장형 문구 제거"는 ui-ux-ai-slop-audit에서 대부분 처리됨 — 신규 문구 작성 시 "1줄 핵심 + 1줄 도움말" 패턴 유지.

### 4-3. 디자이너 관점 기대효과

- 문서-구현 일치 → 이후 모든 화면 작업이 토큰 참조만으로 일관성 확보
- 공공 이용자 기준 가독성·접근성 확보 → 기관 공식화 시 재작업 최소화
- 다크/틸 잔재 제거로 "행정 도구" 정체성 명확화

---

## 5. 통합 로드맵

### 5-1. 우선순위 매트릭스

| 순위 | 과제 | 관점 | 규모 | 선행 조건 |
|------|------|------|------|-----------|
| 1 | 데이터 기준일·출처·면책 고지 3종 표기 | 담당자 | 0.5일 | 없음 |
| 2 | 잔여 기술부채 정리 (type:module, 중첩 폴더, 빈 폴더, 이모지 라벨) | 개발자 | 0.5일 | 없음 |
| 3 | 아키텍처 단일화 결단 + unified-map 베타 노출 | 개발자·디자이너 | 결단 즉시 / 노출 1일 | 실 키 스모크 |
| 4 | 실 키 스모크 테스트 절차 명문화·1회 수행 | 개발자 | 0.5일 | 카카오 JS 키 |
| 5 | GitHub Actions CI (구문·테스트·민감파일 게이트) | 개발자·담당자 | 1일 | 2번 완료 |
| 6 | index.html 랜딩 재도색 + 얇은 허브 전환 | 디자이너 | 1일 | 1번 완료 |
| 7 | schools/infrastructure 기능의 unified-map 흡수 | 개발자·디자이너 | 3~5일 | 3·4번 완료 |
| 8 | 구형 공개 페이지 리다이렉트·제거 | 개발자 | 0.5일 | 7번 완료 |
| 9 | 접근성 대비 실측·모바일 터치 검증 | 디자이너 | 1일 | 7번 진행 중 병행 |
| 10 | 데이터 갱신 절차서 + 연 2회 갱신 사이클 가동 | 담당자 | 0.5일 + 정기 | 1번 완료 |
| — | Phase 2 지오코딩 프록시 | 개발자 | 보류 | 트리거(신규 주소 50건+/회) 충족 시 |
| — | ~~기관 공식화(B안)~~ | 담당자 | **결정 종결(D1)** | A안 확정 — 재검토 시점을 별도로 정하지 않음 |

### 5-2. 단계별 완료 기준

| 단계 | 기간 | 완료 기준 |
|------|------|-----------|
| 1단계 (안정화) | 1~2주 | 순위 1~5 완료. CI 녹색, 고지 3종 화면 노출, 실 키 렌더링 검증 기록 존재 |
| 2단계 (단일화) | 1~2개월 | 순위 6~9 완료. 공개 지도가 unified-map 계열로 일원화, 구형 공개 페이지 0건, DESIGN.md 체크리스트 전 항목 이행 |
| 3단계 (정착) | 분기 반복 | 순위 10 가동. 학기별 데이터 갱신 2회/년 실적, 필요 시 Phase 2·공식화 착수 판단 |

---

## 6. 미검증·유의 사항

- 본 보고서의 코드 인용 라인은 2026-07-05 시점 파일 기준이며, 이후 수정 시 어긋날 수 있음
- `fix-recommendations.md`의 1-2(스키마 검증)·2-1(WeakMap)·2-6(innerText) 항목은 반영 여부를 본 점검에서 전수 확인하지 않음 → 순위 2 작업 시 재감사 필요
- 접근성 대비값(4-2 ③)은 계산상 경계 추정이며 실측 도구 검증 전
- 기관 공식화 관련 절차(도메인·보안성 검토 등)는 내부 규정 확인 필요 — 유권해석·부서 협의 사항
