# ice-eduinfo-map(인천교육 공개지도) 고도화 계획

- 수립일: 2026-07-05
- 근거 문서: `improvement-report(2026-07-05).md` (3관점 개선보고서)
- 성격: **실행 계획서** — 본 문서 승인 후 단계별 착수. 계획 수립 시점에 코드 변경 없음

---

## 1. 추진 목적

- 두 세대 아키텍처 병존·디자인 미이행·데이터 출처 미표기 상태를 해소하고, 개인 업무도구로서 **지속 운영 가능한 단일 체계**로 고도화
- 신규 기능 추가가 아닌 **"단일화·이행·공식화(데이터 기준)" 완성**에 자원 집중

## 2. 확정 전제 (변경 시 본 계획 재검토)

| # | 전제 | 내용 |
|---|------|------|
| D1 | 서비스 지위 | 개인 업무도구 유지. 기관 공식 도메인 미사용. 화면에 비공식 고지 표기 |
| D2 | 아키텍처 | 단일화 확정 — 신형(`unified-map.html` + `js/` 모듈) 승격, 구형 공개 페이지 흡수 후 제거 |
| D3 | 디자인 | DESIGN.md 이행 격차 해소 — 단일화 로드맵과 묶어 실행 |
| D4 | 데이터 출처 | 인천광역시교육청 홈페이지 공식 파일 준용 (현재 2026.4.1. 기준). 갱신은 공식 파일 갱신 시점 추종 |
| D5 | CI 반영 (2026-07-05 추가) | 인천교육청 CI에서 파생한 색상 체계(`ice-blue` #0060b0 주조, `ice-orange` #f08020 포인트 등)를 앱 액센트로 채택. **색상은 차용, 심벌마크·로고타입 직접 게재는 금지**(비공식 개인 도구의 CI 오용 방지). 전용서체 '인천교육힘찬'은 라이선스 확인 후 제목 한정·woff2 서브셋으로만 선택 적용. 상세 규정: `DESIGN.md` §8 |

### 확정 고지 문구

> 본 지도는 개인 제작 참고자료로 공식 자료가 아닙니다.
> 학교 현황은 인천광역시교육청 홈페이지 공개 자료(2026.4.1. 기준)를 준용합니다.

## 3. 범위

| 구분 | 내용 |
|------|------|
| 포함 | 공개 페이지 단일화, DESIGN.md 이행, 데이터 거버넌스(고지·갱신 절차), CI·검증 체계, 기술부채 정리 |
| 제외 | Phase 2 지오코딩 프록시(트리거: 신규 주소 50건+/회 충족 시 별도 계획), 기관 공식화(결정 종결), 신규 기능(연결선 고도화·통계 대시보드 등) |
| 유지 | `business.html`·`private-local-map.html`은 로컬 전용으로 존치 — 3단계에서 이관 또는 동결 선언만 결정 |

---

## 4. 단계별 실행 계획

### 1단계 — 안정화 (착수 후 1~2주)

| 과제 | 작업 내용 | 대상 | 규모 | 완료 기준 |
|------|-----------|------|------|-----------|
| T1. 고지 3종 표기 | 확정 문구(§2)를 공개 페이지 헤더/푸터에 삽입 (기준일·출처·면책) | `index.html`, `schools.html`, `infrastructure.html`, `unified-map.html` | 0.5일 | 공개 페이지 전부에서 고지 노출 확인 |
| T2. 기술부채 정리 | ① 앱 루트 `package.json`에 `"type": "module"` 선언 ② 저장소 중첩 폴더(`ice-eduinfo-map.io-main/ice-eduinfo-map.io-main/`) 평탄화 ③ 루트 빈 `tests/`·`.agents/` 정리 ④ `shared.js` 이모지 라벨(❌⏱️ 등) 텍스트 교체 ⑤ `fix-recommendations.md` 미결 3건(1-2 스키마 검증·2-1 WeakMap·2-6 innerText) 재감사 후 문서에 완료/미결 표기 | 루트 구조, `shared.js`, `fix-recommendations.md` | 0.5~1일 | `node --test` 경고 0건, 이모지 라벨 grep 0건, 문서-코드 괴리 0건 |
| T3. 실 키 스모크 절차 | 절차 명문화(실 키 입력 → 타일 렌더 → 마커 클릭 → 필터 1회 → 키 삭제) + 1회 실측 수행·기록 | `pre-deploy-checklist.md` | 0.5일 | 체크리스트에 절차 추가 + 실측 수행 기록 존재 |
| T4. CI 도입 | GitHub Actions 워크플로 1개: `check-syntax.mjs` + `node --test` + 민감파일 게이트(CSV·키 문자열 검출 시 실패) | `.github/workflows/` | 1일 | push 시 자동 실행·녹색, 민감파일 커밋 테스트 시 차단 확인 |
| T5. 데이터 갱신 절차서 | 교육청 공식 파일 준용 갱신 절차 1쪽: 공식 파일 확보 → `build_schools_json.py` 재실행 → 좌표 검수 → 고지 기준일 갱신 → 커밋 | `docs/data-update-guide.md` (신규) | 0.5일 | 절차서 존재 + 담당자 외 제3자가 절차서만으로 갱신 가능 수준 |

**1단계 게이트**: 위 5개 과제 완료 기준 전부 충족 + CI 녹색 → 2단계 착수 승인

### 2단계 — 단일화 + 디자인 이행 (착수 후 1~2개월)

| 과제 | 작업 내용 | 대상 | 규모 | 완료 기준 |
|------|-----------|------|------|-----------|
| T6. 기능 인벤토리 작성 | 구형 `schools.html`·`infrastructure.html`의 전 기능 목록화(필터·검색·팝업·커스텀 학교·내보내기 등) → unified-map 대비 격차표 | 신규 문서 1쪽 | 0.5일 | 격차표 승인 — **기능 누락 방지의 핵심 선행 작업** |
| T7. unified-map 베타 노출 | GNB에 "통합지도(베타)" 링크 추가, 실 키 스모크(T3 절차) 수행 | `shared.js` GNB | 0.5일 | 베타 진입 가능 + 실 키 렌더링 검증 기록 |
| T8. index.html 허브 전환 | 다크(`#181716`)+틸 제거 → DESIGN.md 흰 캔버스·`ice-blue` 아이덴티티·`ice-orange` 포인트·검정 필 버튼 적용(D5). 브랜드 마크는 텍스트 마크 유지, 교육청 심벌 게재 금지. 비전 슬로건은 고지 인근 텍스트 인용 가능. 2개 지도 진입 카드 + 고지 3종만 남긴 1스크린 허브로 축소 | `index.html` | 1일 | DESIGN.md §7 체크리스트 해당 항목 통과 + §8 CI 규정 준수 |
| T8-1. unified-map CI 토큰 교체 | 현행 구 옐로 토큰(`--um-yellow` #ffd02f 계열)을 DESIGN.md §2 신규 `ice-*` 토큰 패밀리로 교체. 선택 강조·키 안내 패널·포커스 링 일괄 적용 | `unified-map.html`, `shared.css` | 0.5일 | 폐기 토큰(#ffd02f, #fff8e0, #4262ff, #2a41b6) grep 0건 |
| T9. schools 기능 흡수 | 격차표(T6) 기준으로 unified-map에 URL 프리셋(`?view=schools`) 및 잔여 기능 이식 | `unified-map.html`, `js/` | 2~3일 | 격차표 전 항목 이식 완료 + `tests/` 통과 |
| T10. infrastructure 기능 흡수 | 동일 방식 (`?view=infra`) | `unified-map.html`, `js/` | 1~2일 | 격차표 전 항목 이식 완료 + `tests/` 통과 |
| T11. 구형 페이지 제거 | `schools.html`·`infrastructure.html` → unified-map 프리셋 URL로 리다이렉트 스텁화 후 차기 배포에서 제거. GNB·README·배포 화이트리스트 동시 갱신 | 구형 2종, `shared.js`, `README.md` | 0.5일 | 구형 URL 접속 시 정상 이동, 문서 정합 |
| T12. 접근성·모바일 검증 | ① 파스텔 칩 명도 대비 실측(`#746019/#fff4c4` 등, 12px micro 기준 AA) ② 색+형태 이중 부호화 적용 ③ 바텀시트 터치 타깃 44px·제스처 충돌 점검(375px) | `unified-map.html`, `shared.css` | 1일 | 대비 실측표 작성, `pre-deploy-checklist.md` 3절 전 항목 통과 |
| T13. DESIGN.md 이행 감사 | §7 마이그레이션 체크리스트 전 항목 최종 대조 (다크 배경·틸 배지·블루 CTA·폐기 토큰·이모지·다크 모달 0건) + §8 CI 규정 감사(심벌 이미지 미게재, CI 원본 자산 미커밋 확인) | 전체 | 0.5일 | 체크리스트 전 항목 "이행 완료" 표기 |
| T13-1. 전용서체 적용 검토 (선택) | '인천교육힘찬' 배포 라이선스 확인 → 허용 시 제목 글리프 한정 woff2 서브셋 생성·브랜드 타이틀 적용. 미허용·보류 시 Pretendard 700 유지 | 서체 빌드, `shared.css` | 0.5일 | 라이선스 확인 기록 + (적용 시) 서브셋 용량 100KB 이하 |

**2단계 게이트**: 공개 지도가 unified-map 계열로 일원화(구형 공개 페이지 0건) + DESIGN.md 전 항목 이행 + CI 녹색 + 실 키 스모크 재수행

### 3단계 — 정착 (분기 반복)

| 과제 | 작업 내용 | 주기 | 완료 기준 |
|------|-----------|------|-----------|
| T14. 데이터 갱신 사이클 | 교육청 홈페이지 공식 파일 갱신 확인 → T5 절차서에 따라 갱신. 기본 확인 시점: 3.1./9.1. 학기 개시 직후 | 연 2회 | 갱신 실적 기록 (기준일 고지 문구 동기화 포함) |
| T15. 로컬 전용 페이지 처리 결정 | `business.html`·`private-local-map.html` → unified-map 이관 또는 동결 선언 중 택일 | 1회 | 결정 기록 + 동결 시 README에 명시 |
| T16. 보류 과제 트리거 감시 | Phase 2 지오코딩 프록시: 갱신 1회당 좌표 없는 신규 주소 50건 이상 발생 시 별도 계획 수립 | 갱신 시마다 | 트리거 미충족 시 무조치 |

---

## 5. 일정 총괄

| 단계 | 상대 일정 | 투입 규모(순작업) | 게이트 |
|------|-----------|------------------|--------|
| 1단계 안정화 | 1~2주차 | 약 3~3.5일 | 5개 과제 완료 + CI 녹색 |
| 2단계 단일화·디자인 | 3주차~2개월 | 약 8~10일 (CI 토큰 교체 T8-1·서체 검토 T13-1 포함) | 구형 공개 페이지 0건 + DESIGN.md §7·§8 전 항목 이행 |
| 3단계 정착 | 이후 분기 반복 | 갱신 1회당 0.5일 | 연 2회 갱신 실적 |

- 순작업 합계 약 11~13.5일. 실제 달력 일정은 병행 업무를 고려해 단계별 게이트 통과 기준으로 관리 (날짜 고정 아님)

## 6. 검증 체계 (전 단계 공통)

| 검증 | 도구·방법 | 시점 |
|------|-----------|------|
| 구문 | `node tools/check-syntax.mjs` | 매 커밋 (CI) |
| 단위테스트 | `node --test tests/*.test.mjs` | 매 커밋 (CI) |
| 민감파일 게이트 | CI grep (업무용 CSV·키 문자열) | 매 커밋 (CI) |
| 브라우저 QA | fake-kakao 통합 QA (`run-integrated-static-qa.mjs`) | 단계 게이트 시 |
| 실 키 스모크 | T3 절차 (실측 5분) | 배포 직후 매회 |
| 수동 점검 | `pre-deploy-checklist.md` 1~4절 | 단계 게이트 시 |

## 7. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|--------|------|------|
| 구형 페이지 기능 누락 이식 | 실사용 기능 상실 | T6 기능 인벤토리를 이식 착수 전 필수 선행 — 격차표 없이 T9·T10 착수 금지 |
| 실 키 도메인 등록 불일치 | 지도 렌더 실패 | 카카오 Developers Web 플랫폼 등록 도메인(`newskool4d.github.io`, `localhost:8080`) 사전 확인 후 T7 착수 |
| 중첩 폴더 평탄화 시 이력 훼손 | git 히스토리 추적 곤란 | `git mv` 사용 + 평탄화 직전 태그 생성. OneDrive 동기화 폴더 특성상 대량 이동 시 동기화 일시중지 후 작업 |
| 구형 URL 북마크 사용자 | 접속 단절 | T11에서 즉시 삭제 대신 리다이렉트 스텁 1배포 주기 유지 |
| 배포 화이트리스트 누락 | 민감 파일 공개 | T4 CI 게이트 + README 공개 배포 표를 단일 기준으로 유지 |
| 교육청 CI 오용 | 비공식 도구가 공식 채널로 오인 | DESIGN.md §8 "색상 차용 O, 심벌·로고 게재 X" 규정 준수. CI 원본 자산(EPS·TTF)은 저장소 밖 유지, T13 감사 항목으로 확인 |
| 전용서체 라이선스 미확인 적용 | 저작권 문제 | T13-1에서 배포 라이선스 확인 전 적용 금지. 확인 불가 시 Pretendard 유지 |

## 8. 산출물 목록

| 산출물 | 생성 단계 |
|--------|-----------|
| 고지 3종 반영 화면 | 1단계 (T1) |
| `.github/workflows/` CI 워크플로 | 1단계 (T4) |
| `docs/data-update-guide.md` 갱신 절차서 | 1단계 (T5) |
| 기능 인벤토리·격차표 | 2단계 (T6) |
| 접근성 대비 실측표 | 2단계 (T12) |
| DESIGN.md 이행 감사 결과 | 2단계 (T13) |
| 데이터 갱신 실적 기록 | 3단계 (T14, 반복) |

## 9. 착수 조건

- 본 계획 승인 시 **1단계 T1(고지 3종)·T2(기술부채)**부터 착수
- 각 단계 게이트 통과 결과를 확인 후 다음 단계 진행 (게이트 미충족 시 보완 후 재검증)
